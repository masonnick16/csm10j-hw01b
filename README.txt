It starts out by asking what the user wants to do.
if they choose 1, it makes an array with only the prices of the same name and finds the avg, min and max
choosing 2 and 3 with both make arrays of all numbers in the txt file. they will then find their respective value and return that value back into the case.
inside the cases, it finds the name corresponding to the price value and prints out the name and value.
c will rename the file 
q will quit the loop and end the program as it make the loop false.